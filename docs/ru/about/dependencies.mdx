---
title: 'Зависимости'
description: 'Внешние зависимости и библиотеки, используемые в Терем'
icon: 'cube'
---

# Зависимости

Терем использует минимальное количество внешних зависимостей для обеспечения максимальной производительности и надежности.

## Go модули

### Основные зависимости

<CodeGroup>
```go go.mod
module github.com/qzeleza/terem

go 1.25.0

require (
    github.com/charmbracelet/lipgloss v1.1.0
    github.com/qzeleza/termos v1.2.0
    github.com/qzeleza/zlogger v1.0.2
    github.com/spf13/cobra v1.10.1
)
```

```bash Установка
go mod download
go mod verify
```
</CodeGroup>

### Описание модулей

#### 🎨 charmbracelet/lipgloss
- **Назначение**: Стилизация TUI интерфейса
- **Версия**: v1.1.0
- **Лицензия**: MIT
- **Функции**: 
  - Цветовое оформление
  - Рамки и отступы
  - Адаптивная верстка

#### 📟 qzeleza/termos
- **Назначение**: Построение TUI приложений
- **Версия**: v1.2.0
- **Лицензия**: MIT
- **Функции**:
  - Меню и диалоги
  - Прогресс-бары
  - Формы ввода
  - Таблицы данных

#### 📝 qzeleza/zlogger
- **Назначение**: Логирование
- **Версия**: v1.0.2
- **Лицензия**: MIT
- **Функции**:
  - Многоуровневое логирование
  - Ротация логов
  - Цветной вывод

#### 🚀 spf13/cobra
- **Назначение**: CLI фреймворк
- **Версия**: v1.10.1
- **Лицензия**: Apache 2.0
- **Функции**:
  - Парсинг аргументов
  - Подкоманды
  - Автодополнение
  - Генерация документации

## Системные зависимости

### Обязательные утилиты

<Table>
  <thead>
    <tr>
      <th>Утилита</th>
      <th>Пакет</th>
      <th>Назначение</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>bash/sh</td>
      <td>bash</td>
      <td>Выполнение скриптов</td>
    </tr>
    <tr>
      <td>ip</td>
      <td>iproute2</td>
      <td>Управление сетью</td>
    </tr>
    <tr>
      <td>iptables</td>
      <td>iptables</td>
      <td>Настройка фаервола</td>
    </tr>
    <tr>
      <td>ubus</td>
      <td>ubus</td>
      <td>OpenWrt API</td>
    </tr>
  </tbody>
</Table>

### Опциональные утилиты

<AccordionGroup>
  <Accordion title="Сетевые утилиты">
    ```bash
    # Диагностика сети
    opkg install netcat nmap traceroute mtr
    
    # Мониторинг трафика
    opkg install iftop nethogs vnstat
    
    # DNS утилиты
    opkg install bind-dig bind-host
    ```
  </Accordion>
  <Accordion title="Утилиты безопасности">
    ```bash
    # VPN
    opkg install wireguard-tools openvpn
    
    # Шифрование
    opkg install openssl gnupg
    
    # Анализ безопасности
    opkg install fail2ban
    ```
  </Accordion>
  <Accordion title="Системные утилиты">
    ```bash
    # Мониторинг
    opkg install htop iostat
    
    # Архиваторы
    opkg install tar gzip bzip2 xz
    
    # Текстовые редакторы
    opkg install nano vim
    ```
  </Accordion>
</AccordionGroup>

## Версионная совместимость

### Матрица совместимости

| Терем версия | Go версия | termos | zlogger | cobra |
|--------------|-----------|--------|---------|-------|
| 1.0.x | 1.25+ | 1.2.0 | 1.0.2 | 1.10.1 |
| 0.9.x | 1.21+ | 1.1.0 | 1.0.0 | 1.8.0 |
| 0.8.x | 1.19+ | 1.0.0 | 0.9.0 | 1.7.0 |

<Warning>
  Использование несовместимых версий зависимостей может привести к непредсказуемому поведению приложения.
</Warning>

## Установка зависимостей

### Автоматическая установка
```bash
# Скрипт установит все необходимые зависимости
curl -sSL https://terem.zeleza.com/install.sh | sh
```

### Ручная установка

<Steps>
  <Step title="Обновите репозитории">
    ```bash
    opkg update
    ```
  </Step>
  <Step title="Установите базовые пакеты">
    ```bash
    opkg install bash coreutils-base procps-ng
    ```
  </Step>
  <Step title="Установите сетевые утилиты">
    ```bash
    opkg install iproute2 iptables ipset
    ```
  </Step>
  <Step title="Установите опциональные пакеты">
    ```bash
    opkg install curl wget jq nano
    ```
  </Step>
</Steps>

## Проверка зависимостей

### Скрипт проверки
```bash
#!/bin/bash

echo "Проверка зависимостей Терем..."

# Проверка обязательных утилит
REQUIRED=("bash" "ip" "iptables")
MISSING=()

for cmd in "${REQUIRED[@]}"; do
    if ! command -v $cmd &> /dev/null; then
        MISSING+=($cmd)
    fi
done

if [ ${#MISSING[@]} -eq 0 ]; then
    echo "✅ Все обязательные зависимости установлены"
else
    echo "❌ Отсутствуют: ${MISSING[*]}"
    exit 1
fi

# Проверка опциональных утилит
OPTIONAL=("curl" "wget" "jq")
for cmd in "${OPTIONAL[@]}"; do
    if command -v $cmd &> /dev/null; then
        echo "✅ $cmd установлен"
    else
        echo "⚠️ $cmd не установлен (опционально)"
    fi
done
```

## Решение проблем

### Частые проблемы

<Tabs>
  <Tab title="Отсутствующие пакеты">
    ```bash
    # Обновите список пакетов
    opkg update
    
    # Если пакет не найден, проверьте репозитории
    cat /etc/opkg/customfeeds.conf
    
    # Добавьте недостающие репозитории
    echo "src/gz custom http://repo.url" >> /etc/opkg/customfeeds.conf
    ```
  </Tab>
  <Tab title="Конфликты версий">
    ```bash
    # Проверьте установленные версии
    opkg list-installed | grep package_name
    
    # Удалите конфликтующие пакеты
    opkg remove --force-depends package_name
    
    # Установите правильную версию
    opkg install package_name=version
    ```
  </Tab>
  <Tab title="Недостаток места">
    ```bash
    # Проверьте свободное место
    df -h
    
    # Очистите кэш пакетов
    rm -rf /tmp/opkg-lists/*
    
    # Удалите ненужные пакеты
    opkg remove package_name
    ```
  </Tab>
</Tabs>

<Info>
  Терем автоматически проверяет наличие критических зависимостей при запуске и предложит установить отсутствующие компоненты.
</Info>